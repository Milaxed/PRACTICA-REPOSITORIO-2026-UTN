## GITFLOW
Con gitfow aprenderemos a manejar un repositorio colaboratorio.

Nunca trabajamos sobre la rama main para agregar cambios, ya que estos pueden tener errores para eso creamos una rama llamda dev o development

para crear la rama de desarrolo debemos usar
    git checkout -b <nombre-nueva-rama>